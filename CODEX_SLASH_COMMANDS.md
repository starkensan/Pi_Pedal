# Codex スラッシュコマンドまとめ

このドキュメントは、Codex CLI の対話モードで使える `/` から始まるスラッシュコマンドをまとめたものです。

- 主な出典: OpenAI 公式ドキュメント「Slash commands in Codex CLI」
- 確認日: 2026-04-29
- 参考: `codex-cli 0.125.0`

公式ドキュメント:

- https://developers.openai.com/codex/cli/slash-commands

## 基本

対話モードで `/` を入力すると、スラッシュコマンドの候補を開けます。

```bash
codex
```

- スラッシュコマンドは対話モード専用です。
- `codex exec` のような非対話モードでは、通常はそのまま文字列として扱われます。
- タスク実行中でも、コマンドによっては `Tab` で次ターンにキューできます。

## よく使うコマンド

### セッション操作

- `/clear`: 画面をクリアして新しい会話を開始
- `/new`: 同じ CLI セッション内で新しい会話を開始
- `/resume`: 保存済み会話を再開
- `/fork`: 現在の会話を分岐
- `/quit`: CLI を終了
- `/exit`: CLI を終了（`/quit` と同じ）

### モデルと動作

- `/model`: 使用モデルや推論強度を変更
- `/fast`: Fast mode の on/off/status 切り替え
- `/personality`: 応答スタイルを変更
- `/plan`: Plan mode に切り替え、必要ならそのまま計画を指示
- `/permissions`: 承認ポリシーを変更
- `/status`: 現在のモデル、承認設定、トークン使用状況などを表示

### 作業補助

- `/diff`: Git の差分を表示
- `/review`: ワーキングツリーをレビュー
- `/compact`: 会話を要約してコンテキストを節約
- `/copy`: 直近の完了済み出力をコピー
- `/mention`: ファイルやフォルダを会話に添付
- `/init`: `AGENTS.md` のひな形を生成

### 拡張機能まわり

- `/apps`: Apps / connectors を参照してプロンプトに挿入
- `/plugins`: インストール済み・発見可能なプラグインを表示
- `/mcp`: 利用可能な MCP ツールを表示
- `/agent`: サブエージェントのスレッドを切り替え

### 設定・診断

- `/debug-config`: 設定レイヤーやポリシーの診断情報を表示
- `/statusline`: ステータスライン表示項目を設定
- `/title`: ターミナルタイトル表示項目を設定
- `/experimental`: 実験的機能の切り替え
- `/feedback`: ログ送信などのフィードバック用途

### バックグラウンド端末

- `/ps`: バックグラウンド端末の状態表示
- `/stop`: バックグラウンド端末を停止

## コマンド一覧

| コマンド | 概要 | 主な用途 |
| --- | --- | --- |
| `/permissions` | 承認ポリシーを変更 | 自動実行の強さを変えたいとき |
| `/sandbox-add-read-dir` | 追加の読み取りディレクトリを許可 | Windows で追加パスを読ませたいとき |
| `/agent` | アクティブなエージェントスレッドを切り替え | サブエージェントの会話を見たいとき |
| `/apps` | Apps / connectors を参照 | 接続先をプロンプトに付けたいとき |
| `/plugins` | プラグイン一覧を表示 | 利用可能なプラグインを確認したいとき |
| `/clear` | 画面をクリアして新しい会話を開始 | 見た目も会話もリセットしたいとき |
| `/compact` | 会話を要約して圧縮 | 長い会話でコンテキスト節約したいとき |
| `/copy` | 直近の完了済み出力をコピー | 出力をすぐ貼り付けたいとき |
| `/diff` | Git 差分を表示 | 変更内容を確認したいとき |
| `/exit` | CLI 終了 | `/quit` の別名 |
| `/experimental` | 実験的機能を切り替え | 試験機能を有効化したいとき |
| `/feedback` | フィードバック送信 | ログや診断を送りたいとき |
| `/init` | `AGENTS.md` を生成 | 永続的な指示ファイルを作りたいとき |
| `/logout` | ログアウト | 認証情報を外したいとき |
| `/mcp` | MCP ツール一覧を表示 | 外部ツール接続を確認したいとき |
| `/mention` | ファイルやフォルダを添付 | 特定ファイルを読ませたいとき |
| `/model` | モデルを変更 | モデルや reasoning を切り替えたいとき |
| `/fast` | Fast mode 切り替え | 速度優先にしたいとき |
| `/plan` | Plan mode へ切り替え | 実装前に計画を立てたいとき |
| `/personality` | 応答スタイルを変更 | 説明の雰囲気を変えたいとき |
| `/ps` | バックグラウンド端末を表示 | 長時間実行コマンドの様子を見たいとき |
| `/stop` | バックグラウンド端末を停止 | 実行中ジョブを止めたいとき |
| `/fork` | 会話を分岐 | 別案を並行で試したいとき |
| `/resume` | 保存済み会話を再開 | 前回作業の続きをしたいとき |
| `/new` | 新しい会話を開始 | 同じ CLI 内で別タスクに移りたいとき |
| `/quit` | CLI 終了 | セッションを閉じたいとき |
| `/review` | ワーキングツリーをレビュー | 変更の問題点を洗いたいとき |
| `/status` | 現在設定と利用状況を表示 | モデルや承認設定を確認したいとき |
| `/debug-config` | 設定診断を表示 | `config.toml` の効き方を確認したいとき |
| `/statusline` | ステータスライン設定 | 下部表示を調整したいとき |
| `/title` | ターミナルタイトル設定 | タイトル表示を調整したいとき |

## エイリアスと補足

- `/exit` は `/quit` と同じです。
- `/approvals` はまだエイリアスとして使えますが、公式ドキュメントでは「スラッシュ候補一覧には表示されない」とされています。
- `/clean` は `/stop` のエイリアスです。
- `/sandbox-add-read-dir` は Windows ネイティブ実行時のみ利用可能です。
- `/personality` は、使用中モデルが personality 指示をサポートしない場合は表示されないことがあります。
- `/plan` は、タスク実行中は一時的に使えない場合があります。

## よく使う例

### モデルを切り替える

```text
/model
```

### 現在の設定を確認する

```text
/status
```

### 長い会話を圧縮する

```text
/compact
```

### 差分を見る

```text
/diff
```

### 新規会話に切り替える

```text
/new
```

### `AGENTS.md` を作る

```text
/init
```

## まず覚えるならこの5つ

- `/model`
- `/status`
- `/diff`
- `/compact`
- `/init`

## 補足

- スラッシュコマンドは Codex CLI のバージョンで増減する可能性があります。
- 正式な最新情報は、必ず公式ドキュメントを確認してください。
