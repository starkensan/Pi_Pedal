Project-wide public headers are placed in this directory.

Current shared headers:

- `config.h`: firmware-wide pin and feature configuration
- `unity_config.h`: Unity test configuration entry

Policy:

- Keep common headers here when they are used across `src/`, `lib/`, and `test/`.
- Do not add these shared headers under `lib/config/`.
